<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Bloom</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&display=swap');

        :root {
            --rose: #ff5f8f;
            --rose-dark: #d94571;
            --peach: #ffd1dc;
            --ink: #1f1b2c;
            --card: rgba(255, 255, 255, 0.75);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: 'Space Grotesk', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: radial-gradient(circle at top, #ffe0ea 0%, #ffd1dc 35%, #f6b0c2 70%, #f18cb0 100%);
            color: var(--ink);
        }

        .page {
            min-height: 100vh;
            padding: 3.5rem clamp(1rem, 5vw, 4rem) 4rem;
        }

        .hero {
            max-width: 960px;
            margin: 0 auto 2.5rem;
            text-align: center;
        }

        .hero h1 {
            margin: 0;
            font-size: clamp(2.5rem, 6vw, 4.25rem);
            font-weight: 600;
            letter-spacing: -0.02em;
        }

        .hero p {
            margin: 0.75rem auto 0;
            max-width: 540px;
            color: rgba(31, 27, 44, 0.75);
            line-height: 1.6;
        }

        .gallery {
            column-gap: 1.5rem;
            columns: 320px;
        }

        .card,
        .upload-card {
            display: flex;
            flex-direction: column;
            margin: 0 0 1.5rem;
            padding: 1.2rem;
            border-radius: 24px;
            background: var(--card);
            box-shadow: 0 20px 60px rgba(255, 95, 143, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(8px);
            break-inside: avoid;
            position: relative;
            overflow: hidden;
        }

        .card::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255, 95, 143, 0.18), rgba(255, 255, 255, 0));
            pointer-events: none;
        }

        .card h3 {
            margin: 0 0 0.75rem;
            font-size: 1.15rem;
            font-weight: 600;
        }

        .card-media {
            width: 100%;
            border-radius: 18px;
            overflow: hidden;
            background: rgba(255, 255, 255, 0.6);
            border: 1px solid rgba(255, 95, 143, 0.15);
        }

        .card-media img {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            transition: transform 0.6s ease;
        }

        .card:hover .card-media img {
            transform: scale(1.05);
        }

        .card-actions {
            display: flex;
            justify-content: flex-end;
            margin-top: 0.85rem;
        }

        button,
        label.upload-label {
            border: none;
            border-radius: 999px;
            padding: 0.65rem 1.4rem;
            font-weight: 600;
            font-size: 0.95rem;
            cursor: pointer;
            transition: transform 0.25s ease, box-shadow 0.25s ease;
            background: linear-gradient(135deg, var(--rose), var(--rose-dark));
            color: #fff;
            box-shadow: 0 12px 30px rgba(217, 69, 113, 0.35);
        }

        button:hover,
        label.upload-label:hover {
            transform: translateY(-2px);
            box-shadow: 0 18px 35px rgba(217, 69, 113, 0.45);
        }

        button:active,
        label.upload-label:active {
            transform: translateY(0);
            box-shadow: 0 8px 18px rgba(217, 69, 113, 0.3);
        }

        .upload-card {
            text-align: center;
            align-items: center;
            gap: 0.9rem;
        }

        .upload-card h3 {
            margin: 0;
            font-size: 1.1rem;
            font-weight: 600;
        }

        .input {
            width: 100%;
            border: 1px solid rgba(255, 95, 143, 0.35);
            border-radius: 16px;
            padding: 0.75rem 1rem;
            background: rgba(255, 255, 255, 0.85);
            font: inherit;
            color: var(--ink);
        }

        .input::file-selector-button {
            border: none;
            margin-right: 1rem;
            padding: 0.45rem 0.85rem;
            border-radius: 999px;
            background: rgba(255, 95, 143, 0.15);
            color: var(--rose-dark);
            cursor: pointer;
        }

        @media (max-width: 640px) {
            .page {
                padding: 2.5rem 1.25rem 3rem;
            }

            .gallery {
                columns: 1;
            }
        }
    </style>
</head>

<body>
    <div class="page">
        <header class="hero">
            <h1>Image Bloom</h1>
            <p>Curate your own rose-tinted mood board. Upload dreamy shots, fan art, or snippets of daily life and watch them fall into a soft, Pinterest-inspired cascade.</p>
        </header>

        <section class="gallery">
            <% images.forEach(image => { %>
                <article class="card">
                    <h3><%= image.name %></h3>
                    <div class="card-media">
                        <img src="<%= image.imageURL %>" alt="<%= image.name %>">
                    </div>
                    <div class="card-actions">
                        <form action="/upload/<%= image.id %>?_method=DELETE" method="post">
                            <button type="submit">Delete</button>
                        </form>
                    </div>
                </article>
            <% }); %>

            <form class="upload-card" method="post" enctype="multipart/form-data" action="/upload">
                <h3>Add something rosy</h3>
                <input class="input" type="text" name="name" placeholder="Give it a dreamy title" required />
                <label class="upload-label input">
                    <span>Choose an image</span>
                    <input type="file" name="image" accept="image/*" hidden required />
                </label>
                <button type="submit">Upload</button>
            </form>
        </section>
    </div>
</body>

</html>